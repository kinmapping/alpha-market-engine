# strategy 固有の環境変数
# このファイルを .env にコピーして使用してください
# 共有環境変数（REDIS_URL, LOG_LEVEL など）はプロジェクトルートの .env で管理

# 戦略名（例: moving_average_cross）
STRATEGY_NAME=moving_average_cross

# HTTP API を有効化するか（true/false）
ENABLE_HTTP=false

# HTTP API のポート番号（ENABLE_HTTP=true の場合）
HTTP_PORT=8000

